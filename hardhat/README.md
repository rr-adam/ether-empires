# EtherEmpires Hardhat

Here you can find the Hardhat project of EtherEmpires.


```shell
# compile contracts
npm run compile

# start local hardhat node
npm run hardhat

# deploy the contracts on the local hardhat node
npm run deploy-localhost
```
